# CallGuardAI
Call Guard AI is an Android app for near real-time scam call detection: it records short audio chunks, transcribes them (STT), scores risk using an AI endpoint, and shows the threat level via floating overlay widgets, with audio upload testing and basic call-history logs.
